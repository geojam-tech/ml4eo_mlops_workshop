# GFW API token — only needed to run monitoring over dates OUTSIDE the committed
# AIS seed. The default window is fully seeded and runs token-free, so leave this
# blank. For live or out-of-seed dates, get a token at
# https://globalfishingwatch.org/our-apis/tokens and paste it after the '=' below.
GFW_TOKEN=
