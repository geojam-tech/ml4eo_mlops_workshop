apiVersion: 1

datasources:
  - name: ML4EO Metrics
    uid: ml4eo-sqlite
    type: frser-sqlite-datasource
    access: proxy
    jsonData:
      path: /data/metrics.db
    isDefault: true
    editable: true
